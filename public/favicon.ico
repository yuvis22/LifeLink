// This is a placeholder for favicon.ico
// In a real project, you would need to create an actual .ico file
// You can convert the SVG to ICO using a tool like https://convertio.co/svg-ico/
// or using ImageMagick with a command like:
// convert favicon.svg -define icon:auto-resize=16,32,48,64,128,256 favicon.ico 